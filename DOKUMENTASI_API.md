# Dokumentasi API INVENDAM

API ini digunakan untuk mengakses data sistem inventory PDAM dalam format JSON.  
API menggunakan autentikasi token Bearer dari Laravel Sanctum.

Base URL:

```txt
http://127.0.0.1:8000/api